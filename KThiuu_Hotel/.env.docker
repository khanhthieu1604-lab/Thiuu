# =================================================================
# KThiuu Hotel - Docker Environment Configuration
# =================================================================

# --- APPLICATION ---
APP_NAME="KThiuu Hotel"
APP_ENV=local
APP_KEY=base64:3nj4ZecjYyTkDCuLk1IrXIg5oBpJh7grvND3lYQciKs=
APP_DEBUG=true
APP_URL=http://localhost:8085

APP_LOCALE=vi
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=vi_VN

APP_MAINTENANCE_DRIVER=file
BCRYPT_ROUNDS=12

# --- LOGGING ---
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# --- DATABASE (Docker MySQL) ---
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=kthiuu_hotel
DB_USERNAME=laravel
DB_PASSWORD=password

# --- SESSION ---
SESSION_DRIVER=redis
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_PATH=/
SESSION_DOMAIN=null

# --- CACHE & QUEUE ---
BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=redis
CACHE_STORE=redis

# --- REDIS (Docker) ---
REDIS_CLIENT=phpredis
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

# --- MAIL (Mailhog Docker) ---
MAIL_MAILER=smtp
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="bookings@kthiuu-hotel.com"
MAIL_FROM_NAME="${APP_NAME}"

# --- PAYMENT GATEWAY ---
VNPAY_TMN_CODE=
VNPAY_HASH_SECRET=
VNPAY_URL=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
VNPAY_RETURN_URL="${APP_URL}/payment/vnpay/return"

# --- VITE ---
VITE_APP_NAME="${APP_NAME}"

# --- LIÊN KẾT THIUU ECOSYSTEM ---
KTHIUU_HOTEL_URL=http://localhost:8001
THIUU_CARRENTAL_URL=http://localhost:8000

# --- SHARED AUTH DATABASE (SSO) ---
SHARED_AUTH_DB_HOST=shared_auth_db
SHARED_AUTH_DB_PORT=3306
SHARED_AUTH_DB_DATABASE=thiuu_auth
SHARED_AUTH_DB_USERNAME=thiuu_auth
SHARED_AUTH_DB_PASSWORD=thiuu_secret
